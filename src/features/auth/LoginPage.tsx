// TODO: registrazione/login con profilo personale (username + foto profilo)
// su account Supabase individuale — vedi PROMPT_REACT_REWRITE.md,
// "Modello di camere, identità e permessi" e lezione 1.
export function LoginPage() {
  return (
    <section>
      <h1>Accedi</h1>
      <p>Placeholder — form di login/registrazione da implementare.</p>
    </section>
  )
}
